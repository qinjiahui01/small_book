const host = 'http://localhost:9797'
const config = {
  host,
  login: `${host}/webapp/login`,
  user: `${host}/webapp/user`,
  top: `${host}/webapp/top`,
  bookList: `${host}/webapp/booklist`,
  commentlist: `/webapp/commentList`,
  addBook: `${host}/webapp/addBook`,
  bookDetail: `${host}/webapp/bookDetail`,
  commentList: `${host}/webapp/commentList`,
  addComment: `${host}/webapp/addComment`
}

export default config
